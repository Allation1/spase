{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Open Game Preview",
            "command": "${command:livePreview.start.preview.at.url}",
            "args": [
                "http://localhost:3000"
            ],
            "problemMatcher": [],
            "presentation": { "reveal": "never" }
        }
    ]
}